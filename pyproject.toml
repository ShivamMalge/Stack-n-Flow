[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pratyaksha"
version = "0.1.0"
description = "A Python visualization library driven by React and anywidget"
dependencies = [
    "anywidget",
    "traitlets",
]
